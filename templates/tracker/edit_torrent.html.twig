{% extends 'layout/tracker.html.twig' %}

{% block title %}Edit Torrent{% endblock %}

{% block  javascripts %}
    {{ encore_entry_script_tags('edit_torrent') }}
{% endblock %}

{% block body %}
    <div class="row small-device mt-4 mb-5">
        <div class="col-12 col-xl-8 col-lg-10 mx-auto">
            <?php echo isset($data['request_msg']) ? $data['request_msg'] : ""; ?>

            <h1 class="text-center">Edit Torrent</h1>
                <div class="fancy-container bx-d-sm-0">
                {{ form_start(edit_form) }}
                    <!-- torrent name -->
                    <div class="fancy-row">
                        <div class="row small-device">
                            <div class="rowhead col-12 col-sm-2 pr-sm-0 text-left-d-sm pb-d-sm-1 pt-sm-1">{{ form_label(edit_form.name) }}</div>
                            <div class="col-12 col-sm-10">
                                {{ form_widget(edit_form.name) }}
                            </div>
                        </div>
                    </div>

                    <!-- description template dvd -->
                    <div id="desc_dvd" class="fancy-row">
                        <div class="row small-device">
                            <div class="rowhead col-12 col-sm-2 pr-sm-0 text-left-d-sm pb-d-sm-1 pt-1">{{ form_label(edit_form.description) }}</div>
                            <div class="col-12 col-sm-10">
                                <div id="upload-description-template-dvd">
                                    {{ form_widget(edit_form.description) }}
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- content info -->
                    <div class="fancy-row" id="upload-content-info">
                        <div class="row small-device">
                            <div class="rowhead col-12 col-sm-2 pr-sm-0 text-left-d-sm pb-d-sm-1">{{ form_label(edit_form.contentInfo) }}</div>
                            <div class="col-12 col-sm-10" id="upload-format-type-box">
                               {{ form_widget(edit_form.contentInfo) }}
                            </div>
                        </div>
                    </div>

                    <!-- media info -->
                    <div class="fancy-row">
                        <div class="row small-device">
                            <div class="rowhead col-12 col-sm-2 pr-sm-0 text-left-d-sm pb-d-sm-1 pt-1">{{ form_label(edit_form.mediaInfo) }}</div>
                            <div class="col-12 col-sm-10">
                                {{ form_widget(edit_form.mediaInfo) }}
                            </div>
                        </div>
                    </div>

                    <!-- save button -->
                    <div class="fancy-row">
                        <div class="row small-device">
                            <div class="col-12 text-center">
                                <input id="qr" type="submit" name="upload" class="btn" value="Save" />
                                <input type="button" class="btn ml-1" onclick="window.history.back();" value="Cancel" />
                            </div>
                        </div>
                    </div>
                </div>
            {{ form_end(edit_form) }}
        </div>
    </div>
{% endblock %}
